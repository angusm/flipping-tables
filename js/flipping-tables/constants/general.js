/**
 * Created by Angus on 2015-01-25.
 */

// Just contain a bunch of constants so we're not depending on typing
// the same string correctly every time.
define (
	[],
	function() {
		return {
			moduleName: 'FlippingTables'
		}
	}
);